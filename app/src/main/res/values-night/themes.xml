<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.AndroidprgEindopdracht" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="android:editTextColor">@color/primary_text_color</item>
        <item name="android:textColorHint">@color/primary_text_color</item>
        <item name="android:colorAccent">@color/primary_text_color</item>
        <item name="fontFamily">monospace</item>
        <item name="android:textColor">@color/primary_text_color</item>
        <item name="android:background">@color/background_color</item>
    </style>

    <style name="Theme.AndroidprgEindopdracht" parent="Base.Theme.AndroidprgEindopdracht" />
</resources>